# Phase 6 Review — Docs

**Reviewer**: superpowers:code-reviewer (subagent)
**Date**: 2026-04-22
**Commits under review**:
- `da05e1b` docs: add contributing guide
- `a3c4aa0` docs: add architecture doc
- `ccf80a7` docs: add dashboard setup guide
- `aaf4ade` docs: add roadmap
- `6f6ff1c` (README, commit preceding contributing)

**Follow-up fix commit**: `<this PR>`

## Scope

- `README.md`, `CONTRIBUTING.md`
- `docs/architecture.md`, `docs/dashboard-setup.md`, `docs/roadmap.md`

## Findings

### Critical — fixed

- **C-1** README listed `60 d` as an allowed stats window; `const.py`
  has `ALLOWED_STATS_WINDOWS = (1, 7, 14, 30, 90)`. Updated to
  "1 d / 7 d / 30 d / 90 d".
- **C-2** README claimed Silver rules are all `done`; `quality_scale.yaml`
  still has 7 open rules (brands + docs-* pending upstream PRs).
  Softened to "targeting Silver" with context.
- **C-3** CONTRIBUTING referenced `pip install -e .[dev]` but no `[dev]`
  extra exists in `pyproject.toml`. Replaced with `pip install -e .`
  + `pip install -r requirements-test.txt`.
- **C-4** CONTRIBUTING coverage floor was inverted (90 % overall, 95 %
  config_flow). `pyproject.toml` sets `--cov-fail-under=95` globally with
  no per-file floor. Fixed.

### Important — fixed

- **I-1** Architecture named the runtime-data dataclass `NightscoutRuntimeData`
  with 4 fields. Actual: `NightscoutData` with 6 fields (adds `store`,
  `jwt_manager`, `jwt_refresh_unsub`). Corrected + expanded snippet.
- **I-2** Architecture claimed JWT refresh threshold is "< 30 min".
  `api/auth.py:15` has `REFRESH_THRESHOLD_SECONDS = 3600` (60 min). Fixed,
  noting the parallel 7 h pre-refresh scheduler from `const.py`.
- **I-3** Architecture said "13-sensor stats bundle". Actual is 14. Fixed
  + enumerated the 14 features inline.
- **I-4** Error mapping conflated initial-setup and options-rediscover
  paths. Split into two rows (`ConfigEntryNotReady` at setup vs.
  `async_abort(cannot_connect)` in Options).
- **I-5** `strings.json` (and `translations/{en,de}.json`) told users they
  need `api:treatments:create`. The integration is read-only in v0.1.0 —
  that scope is unnecessary. Token description updated in all three files.

### Minor — addressed

- **M-3** CONTRIBUTING dropped the misleading
  `python -m script.hassfest --integration-path …` line; replaced with a
  note that hassfest needs an HA core checkout.
- **M-6** README said `HA ≥ 2026.1`; plan/design said `≥ 2025.1`.
  `runtime_data` has been available since 2024.11 — set README to
  `≥ 2025.1`.
- **M-7** Architecture's "one name key" sentence undersold the
  `translation_placeholders={"window": str(w)}` pattern for stats
  entities; expanded.

### Not an issue

- ProdInstance `FORBIDDEN_HOSTS` guard verified in both
  `scripts/smoke_test.py:18` and `scripts/capture_fixtures.py:26`.
- No URL / token / identifier leaks found in any doc.
- Dashboard example YAML files exist at documented paths; AGP attribute
  contract matches the coordinator output after the Phase 5 fix.

## Verdict

**Approved after review fixes applied.** 168 tests pass, coverage 95.25%.
